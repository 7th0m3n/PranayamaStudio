package com.pranayama.studio.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Pranayama Studio Color Palette
 * 
 * A calm, modern "yogic tech" aesthetic with:
 * - Deep dark backgrounds for focus and calm
 * - Soft teal/cyan as primary accent (breath, life)
 * - Warm amber/gold as secondary accent (energy, warmth)
 * 
 * To customize colors, simply change the hex values below.
 */

// ===== DARK THEME COLORS =====

// Backgrounds - deep charcoal with subtle blue undertone
val DarkBackground = Color(0xFF0D1117)
val DarkSurface = Color(0xFF161B22)
val DarkSurfaceVariant = Color(0xFF21262D)

// Primary - soft teal/cyan (breath, air, life)
val PrimaryTeal = Color(0xFF58A6FF)
val PrimaryTealDark = Color(0xFF388BFD)
val PrimaryTealLight = Color(0xFF79C0FF)

// Secondary - warm amber/gold (energy, warmth)
val SecondaryAmber = Color(0xFFD29922)
val SecondaryAmberLight = Color(0xFFE3B341)
val SecondaryAmberDark = Color(0xFFBB8009)

// Text colors
val TextPrimary = Color(0xFFF0F6FC)
val TextSecondary = Color(0xFF8B949E)
val TextMuted = Color(0xFF6E7681)

// Accent colors for breath phases
val InhaleColor = Color(0xFF58A6FF)    // Bright teal-blue for inhale
val HoldColor = Color(0xFFD29922)       // Warm amber for hold
val ExhaleColor = Color(0xFF7EE787)     // Soft green for exhale

// Success/Error states
val SuccessGreen = Color(0xFF3FB950)
val ErrorRed = Color(0xFFF85149)

// ===== LIGHT THEME COLORS (Optional - for future use) =====
val LightBackground = Color(0xFFF6F8FA)
val LightSurface = Color(0xFFFFFFFF)
val LightSurfaceVariant = Color(0xFFEBEDF0)
